# Numarul ereditar karmic - exemple

Pentru interpretarea completa a rezultatului, vezi
[`03-interpretari-neam.md`](03-interpretari-neam.md).

In unele note de lucru, acelasi rezultat poate aparea sub denumirea `karma
neamului`. In proiect, acest termen este doar un alias pentru numarul ereditar
karmic / numarul neamului.

## Exemplu 1 - Doinas

Nume de familie:

```text
Doinas
```

Valori:

```text
D=4, O=6, I=9, N=5, A=1, S=1
```

Calcul:

```text
4 + 6 + 9 + 5 + 1 + 1 = 26
26 - 22 = 4
```

Rezultat:

```text
numarul ereditar karmic / al neamului = 4
```

Interpretarea se cauta in `03-interpretari-neam.md`, la:

```text
Neamul 4 - Arcana Majora 4 - Imparatul - Negustorii si intreprinzatorii
```

## Exemplu 2 - Popa

Nume de familie:

```text
Popa
```

Valori:

```text
P=7, O=6, P=7, A=1
```

Calcul:

```text
7 + 6 + 7 + 1 = 21
```

Rezultat:

```text
numarul ereditar karmic / al neamului = 21
```

Interpretarea se cauta in `03-interpretari-neam.md`, la:

```text
Neamul 21 - Arcana Majora 21 - Lumea - Afaceristii internationali
```

## Exemplu 3 - S

Nume de familie:

```text
S
```

Valori:

```text
S=1
```

Calcul:

```text
1 = 1
```

Rezultat:

```text
numarul ereditar karmic / al neamului = 1
```

Interpretarea se cauta in `03-interpretari-neam.md`, la:

```text
Neamul 1 - Arcana Majora 1 - Magicianul - Invatatorii elitisti
```

## Exemplu 4 - Constantinescu

Nume de familie:

```text
Constantinescu
```

Calcul:

```text
CONSTANTINESCU = 51
51 - 22 = 29
29 - 22 = 7
```

Rezultat:

```text
numarul ereditar karmic / al neamului = 7
```

Interpretarea se cauta in `03-interpretari-neam.md`, la:

```text
Neamul 7 - Arcana Majora 7 - Carul - Savantii
```

## Exemplu 5 - Popescu

Nume de familie:

```text
Popescu
```

Valori:

```text
P=7, O=6, P=7, E=5, S=1, C=3, U=3
```

Calcul:

```text
7 + 6 + 7 + 5 + 1 + 3 + 3 = 32
32 - 22 = 10
```

Rezultat:

```text
numarul ereditar karmic / al neamului = 10
```

Interpretarea se cauta in `03-interpretari-neam.md`, la:

```text
Neamul 10 - Arcana Majora 10 - Roata Norocului - Contabilii si finantistii
```
